# Core to-do list

- [ ] Constraint Evaluation per each class session
- [ ] Some selector and better config in solver_runner.h
- [ ] Add possibility of resolving the problem with different constraints
- [ ] Better sequence mapper, overall better mappers for missing classes
- [ ] Limitation: when we have same classes with same type, same id twice in a week, lecturers twice in a week break the solver (fix: just change the second id)